name: Deploy to Azure VM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install sshpass
        run: sudo apt-get install -y sshpass  # Install sshpass if not available

      - name: SSH into Virtual Machine
        run: |
          sshpass -p JYm52DbxCDE3AqE ssh -v WiselyDev@172.174.218.96 "echo 'Connected to VM'"

